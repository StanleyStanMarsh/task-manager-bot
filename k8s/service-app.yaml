apiVersion: v1
kind: Service
metadata:
  name: task-manager-bot
  namespace: task-manager-bot
  labels:
    app: task-manager-bot
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  selector:
    app: task-manager-bot
